Update only the MARSA PostgreSQL DEVELOPMENT database for the marketplace
preparation update.

This task is additive only. Use the synced Drizzle schema and migration files as
the source of truth.

SAFETY RULES

1. Confirm this is the Replit Development database, not Production.
2. Do not expose DATABASE_URL or any secret.
3. Do not use:
   - DROP TABLE
   - DROP COLUMN
   - TRUNCATE
   - destructive enum replacement
   - table recreation
   - drizzle-kit push --force
4. Do not rename or change the type/default/meaning of any existing column.
5. Preserve all existing Stripe columns, location columns, booking data, and
   business records.
6. Only create the new tables/indexes below and add the explicitly listed
   columns to existing tables.
7. Review generated SQL before applying it. Stop if it contains an unlisted or
   destructive operation.
8. Do not run the general seed command.
9. Do not insert sample users, yachts, bookings, payments, or cancellation fees.
10. Do not publish or deploy after completing the database update.

Use text IDs generated by the application, timezone-aware timestamps, and the
existing naming conventions.

==================================================
NEW TABLES
==================================================

1. locations

Columns:
- id: text primary key
- name: text not null
- city: text not null
- country: text not null
- time_zone: text not null
- slug: text not null unique
- is_active: boolean not null default true
- is_default: boolean not null default false
- sort_order: integer not null default 0
- created_at: timestamptz not null default now()
- updated_at: timestamptz not null default now()

Constraints and indexes:
- Unique slug.
- Partial unique index allowing only one row where is_default=true.
- Index on (is_active, sort_order).
- time_zone must contain an application-validated IANA time-zone name.

Data:
- Upsert Gouna, Egypt using:
  - name: Gouna
  - city: Gouna
  - country: Egypt
  - time_zone: Africa/Cairo
  - slug: gouna-egypt
  - is_active: true
- If no default location exists, make Gouna the default.
- If another default already exists, preserve it and leave Gouna non-default.
- Do not create an “Other” row. Other is only a UI option.

--------------------------------------------------

2. wishlist_items

Columns:
- id: text primary key
- user_id: text not null, FK users(id), ON DELETE CASCADE
- yacht_id: text not null, FK yachts(id), ON DELETE CASCADE
- created_at: timestamptz not null default now()

Constraints and indexes:
- Unique (user_id, yacht_id).
- Index on user_id.
- Index on yacht_id.

--------------------------------------------------

3. admin_events

Columns:
- id: text primary key
- section_key: text not null
- entity_type: text not null
- entity_id: text not null
- event_type: text not null
- metadata: jsonb nullable
- occurred_at: timestamptz not null default now()

Indexes:
- Index on (section_key, occurred_at).
- Index on (entity_type, entity_id).

Expected section keys include:
- users
- hosts
- documents
- yachts
- bookings
- cancellations
- withdrawals
- reviews
- photographer_requests

Do not enforce entity_id as a foreign key because it may reference different
entity tables.

--------------------------------------------------

4. admin_section_views

Columns:
- admin_user_id: text not null, FK users(id), ON DELETE CASCADE
- section_key: text not null
- last_seen_at: timestamptz not null default now()

Constraints:
- Composite primary key or unique constraint on
  (admin_user_id, section_key).

--------------------------------------------------

5. notification_campaigns

Columns:
- id: text primary key
- title: text not null
- message: text not null
- audience: text not null default 'all'
- status: text not null default 'queued'
- created_by: text not null, FK users(id), ON DELETE RESTRICT
- total_recipients: integer not null default 0
- in_app_sent_count: integer not null default 0
- push_sent_count: integer not null default 0
- push_failed_count: integer not null default 0
- created_at: timestamptz not null default now()
- started_at: timestamptz nullable
- completed_at: timestamptz nullable

Allowed statuses:
- queued
- sending
- completed
- partial_failed
- failed

Constraints and indexes:
- All count columns must be >= 0.
- Index on (status, created_at).

--------------------------------------------------

6. user_push_tokens

Columns:
- id: text primary key
- user_id: text not null, FK users(id), ON DELETE CASCADE
- expo_push_token: text not null
- device_id: text nullable
- platform: text not null
- app_version: text nullable
- is_active: boolean not null default true
- last_registered_at: timestamptz not null default now()
- deactivated_at: timestamptz nullable
- created_at: timestamptz not null default now()
- updated_at: timestamptz not null default now()

Allowed platforms:
- ios
- android

Constraints and indexes:
- Unique expo_push_token.
- Partial unique index on (user_id, device_id) where device_id is not null.
- Index on (user_id, is_active).

--------------------------------------------------

7. notification_deliveries

Columns:
- id: text primary key
- campaign_id: text not null,
  FK notification_campaigns(id), ON DELETE RESTRICT
- user_id: text not null, FK users(id), ON DELETE RESTRICT
- channel: text not null
- push_token_id: text nullable,
  FK user_push_tokens(id), ON DELETE SET NULL
- notification_id: text nullable,
  FK notifications(id), ON DELETE SET NULL
- status: text not null default 'pending'
- attempt_count: integer not null default 0
- provider_reference: text nullable
- last_error: text nullable
- next_attempt_at: timestamptz nullable
- created_at: timestamptz not null default now()
- sent_at: timestamptz nullable

Allowed channels:
- in_app
- push
- email

Allowed statuses:
- pending
- sent
- failed
- skipped

Constraints and indexes:
- attempt_count must be >= 0.
- Unique one in-app delivery per campaign/user.
- Unique one push delivery per campaign/push token.
- Index on campaign_id.
- Index on (status, next_attempt_at).

Keep email as a valid future channel, but do not enqueue email deliveries yet.

--------------------------------------------------

8. cancellation_policies

Columns:
- id: text primary key
- name: text not null
- version: integer not null
- status: text not null default 'draft'
- created_by: text not null, FK users(id), ON DELETE RESTRICT
- activated_at: timestamptz nullable
- retired_at: timestamptz nullable
- created_at: timestamptz not null default now()
- updated_at: timestamptz not null default now()

Allowed statuses:
- draft
- active
- retired

Constraints and indexes:
- version must be positive and globally unique.
- Partial unique index allowing only one policy with status='active'.

Do not insert an active policy during this database task.

--------------------------------------------------

9. cancellation_policy_rules

Columns:
- id: text primary key
- policy_id: text not null,
  FK cancellation_policies(id), ON DELETE CASCADE
- minimum_minutes_before_trip: integer not null
- fee_percentage: decimal(5,2) not null
- created_at: timestamptz not null default now()
- updated_at: timestamptz not null default now()

Constraints and indexes:
- minimum_minutes_before_trip must be >= 0.
- fee_percentage must be between 0 and 100.
- Unique (policy_id, minimum_minutes_before_trip).
- Index on (policy_id, minimum_minutes_before_trip descending).

The application must require a zero-minute catch-all rule before activating a
policy.

Do not insert the example 48-hour, 24-hour, or percentage rules.

--------------------------------------------------

10. booking_cancellation_terms

Columns:
- id: text primary key
- booking_id: text not null unique,
  FK bookings(id), ON DELETE RESTRICT
- policy_id: text not null,
  FK cancellation_policies(id), ON DELETE RESTRICT
- policy_version: integer not null
- policy_name: text not null
- rules_snapshot: jsonb not null
- trip_starts_at: timestamptz not null
- time_zone: text not null
- accepted_at: timestamptz not null
- created_at: timestamptz not null default now()

Do not create terms for existing bookings. New terms will be inserted by the
application when new bookings are created.

--------------------------------------------------

11. booking_cancellations

Columns:
- id: text primary key
- booking_id: text not null, FK bookings(id), ON DELETE RESTRICT
- requested_by: text not null, FK users(id), ON DELETE RESTRICT
- reason: text nullable
- requested_at: timestamptz not null default now()
- booking_status_before_request: text not null
- trip_starts_at: timestamptz not null
- remaining_minutes: integer not null
- policy_id: text nullable,
  FK cancellation_policies(id), ON DELETE RESTRICT
- policy_version: integer nullable
- matched_rule_id: text nullable,
  FK cancellation_policy_rules(id), ON DELETE RESTRICT
- fee_percentage: decimal(5,2) nullable
- original_amount_egp: decimal(12,2) not null
- fee_amount_egp: decimal(12,2) nullable
- refund_amount_egp: decimal(12,2) nullable
- status: text not null default 'pending'
- reviewed_by: text nullable, FK users(id), ON DELETE RESTRICT
- reviewed_at: timestamptz nullable
- review_notes: text nullable
- refund_id: text nullable unique, FK refunds(id), ON DELETE RESTRICT
- created_at: timestamptz not null default now()
- updated_at: timestamptz not null default now()

Allowed statuses:
- pending
- processing
- approved
- rejected
- refund_failed

Constraints and indexes:
- fee_percentage must be null or between 0 and 100.
- Monetary values must be non-negative.
- When fee and refund are both populated:
  fee_amount_egp + refund_amount_egp = original_amount_egp.
- Index on (booking_id, requested_at descending).
- Index on (status, requested_at).
- Partial unique index preventing more than one pending or processing
  cancellation for the same booking.

==================================================
ADDITIVE COLUMNS ON EXISTING TABLES
==================================================

Only add the columns below. Preserve every existing column.

1. yachts

Add:
- location_id: text nullable,
  FK locations(id), ON DELETE SET NULL
- custom_location_name: text nullable
- is_featured: boolean not null default false
- featured_sort_order: integer not null default 0
- featured_from: timestamptz nullable
- featured_until: timestamptz nullable

Constraints and indexes:
- If both featured dates exist, featured_from must be <= featured_until.
- Index supporting public filtering by status and location_id.
- Index supporting featured ordering.

Preserve:
- location
- city
- all existing yacht data and statuses

--------------------------------------------------

2. availability_slots

Add:
- price_override_egp: decimal(12,2) nullable
- hold_expires_at: timestamptz nullable

Constraints and indexes:
- price_override_egp must be null or greater than zero.
- Index on hold_expires_at where it is not null.

Preserve:
- is_available
- all existing slot uniqueness rules
- all existing slot rows

--------------------------------------------------

3. payments

Add:
- provider: text not null default 'stripe'
- provider_payment_id: text nullable
- provider_metadata: jsonb nullable
- is_test: boolean not null default false
- succeeded_at: timestamptz nullable

Constraints and indexes:
- Index on booking_id.
- Partial unique index on (provider, provider_payment_id)
  where provider_payment_id is not null.

Preserve:
- stripe_payment_intent_id
- stripe_charge_id
- receipt_url
- all existing payment rows and statuses

--------------------------------------------------

4. refunds

Add:
- provider: text not null default 'stripe'
- provider_refund_id: text nullable
- is_test: boolean not null default false

Constraints and indexes:
- Partial unique index on (provider, provider_refund_id)
  where provider_refund_id is not null.

Preserve:
- stripe_refund_id
- all existing refund rows and statuses

Also preserve host_profiles.stripe_connect_id unchanged.

==================================================
SAFE BACKFILL
==================================================

Before backfilling:
- Record row counts for users, yachts, availability_slots, bookings, payments,
  refunds, notifications, and every new table.
- Do not print personal data or secrets.

Run the marketplace backfill in a transaction where practical.

Backfill rules:

1. Upsert Gouna, Egypt with slug gouna-egypt and time zone Africa/Cairo.
2. Ensure exactly one active default location:
   - If no default exists, set Gouna as default.
   - If another default exists, preserve it.
3. Set yachts.location_id to Gouna only when the existing city/location clearly
   represents Gouna or El Gouna.
4. Do not relabel non-Gouna yachts.
5. Preserve the existing yachts.location and yachts.city values.
6. For legacy payments:
   - provider='stripe'
   - provider_payment_id=stripe_payment_intent_id when available
   - is_test=false
7. For legacy refunds:
   - provider='stripe'
   - provider_refund_id=stripe_refund_id when available
   - is_test=false
8. Do not change booking prices, totals, statuses, dates, or payment history.
9. Do not create cancellation terms for old bookings.
10. Do not create or activate a cancellation policy.
11. Do not insert the illustrative cancellation percentages.
12. Do not add sample records.
13. The backfill must be idempotent and safe to run repeatedly.

Run the backfill twice and confirm that the second run produces no duplicate or
unexpected changes.

==================================================
PROCEDURE
==================================================

1. Confirm REPLIT_DEPLOYMENT is not 1.
2. Confirm the Replit Database tool shows Development.
3. Record the pre-change row counts.
4. Run:

   pnpm install --frozen-lockfile

5. Preview the Drizzle schema changes.
6. Confirm the preview contains only:
   - creation of the 11 tables above;
   - creation of their indexes and constraints;
   - the explicitly listed additive columns and indexes.
7. If any destructive or unlisted change appears, stop without applying it.
8. Apply with:

   pnpm --filter @workspace/db run push

9. Run:

   pnpm --filter @workspace/scripts run backfill:marketplace-update

10. Run the same backfill a second time.
11. Run:

   pnpm run typecheck
   pnpm --filter @workspace/api-server run test

==================================================
FINAL VERIFICATION
==================================================

Verify and report:

- All 11 new tables exist.
- All listed indexes, foreign keys, uniqueness constraints, and checks exist.
- Exactly one active default location exists.
- Gouna exists with slug gouna-egypt and time zone Africa/Cairo.
- No “Other” location row exists.
- Non-Gouna legacy locations were not overwritten.
- Wishlist uniqueness is enforced.
- Admin unseen state is stored per administrator.
- Notification delivery idempotency constraints exist.
- Email is modeled but no email deliveries were inserted.
- Existing Stripe columns and identifiers remain unchanged.
- Legacy payments/refunds have provider='stripe'.
- No test payment records were created by the migration.
- Only one cancellation policy can be active.
- Cancellation thresholds are unique per policy.
- Only one open cancellation request can exist per booking.
- No cancellation policy or example percentages were inserted.
- No old booking received cancellation terms.
- Existing booking/payment/refund row counts are unchanged.
- The backfill produced the same result on both runs.
- Typecheck and API tests passed.

Report:
- Commands executed
- Tables, columns, indexes, and constraints added
- Before/after row counts
- Backfill results
- Test results
- Any warnings or schema drift

Do not publish, deploy, or modify the Production database.